name: Build and Release
on:
  push:
    branches:
      - main
jobs:
  test-and-release:
    name: Test, Build, and Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint
        run: pnpm lint

      - name: Run tests
        run: pnpm test

      - name: Build project
        run: pnpm build

      - name: Validate skill
        run: |
          test -f skill/SKILL.md || { echo "FAIL: skill/SKILL.md not found"; exit 1; }
          head -1 skill/SKILL.md | grep -q '^---$' || { echo "FAIL: missing frontmatter"; exit 1; }
          grep -q '^name:' skill/SKILL.md || { echo "FAIL: missing name field"; exit 1; }
          grep -q '^description:' skill/SKILL.md || { echo "FAIL: missing description field"; exit 1; }
          echo "Skill validation passed"

      - name: Semantic Release
        run: pnpm exec semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
